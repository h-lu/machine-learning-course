# 发布、版本与课包切换

2026 班第 01–04 课已经授课。学生材料、作业要求、概念题解释和成绩依据继续按当时采用的版本保存，本轮不追溯修改。第 05–32 课按新学习地图成套更新；只有学生任务、教师材料、题库和线上服务属于同一发布版本时，教师才创建对应的新场次。

本轮题库版本为 `ml-v13-course-map-2026-09-21`。每课 A 版、AI 学习、B 版分别为 240、300、240 秒，另留约 2 分钟提交。课程编号映射保持不变：C01、C02 是第 01、02 课，S01–S30 是第 03–32 课。

## 当前采用边界

| 范围 | 课堂状态 | 本轮处理 |
|---|---|---|
| 第 01–04 课（C01、C02、S01、S02） | 已授课 | 冻结学生要求；旧场次保存当时题目快照，不用新题重解释 |
| 第 05–08 课（S03–S06） | 后续可继续使用 | 保持问题与评价主线，加深 S03、S04 的选择与证据要求 |
| 第 09–14 课（S07–S12） | 新课包 | 模型假设 → 概率行动 → 表示 → 参数更新 → 借用表示 → 泛化诊断 |
| 第 15–20 课（S13–S18） | 新课包 | 预训练能力来源 → 信息路径 → 提示 → 检索 → 适配路线 → 开放结果评价 |
| 第 21–26 课（S19–S24） | 新课包 | 模型、程序与人协作的同一服务流程，保留中间产物并作端到端改进 |
| 第 27–32 课（S25–S30） | 新课包 | 反馈 → 奖励 → 监测恢复 → 资源 → 迁移 → 可重跑交付 |

各模块的具体实验、命令演练和冷读范围分别记录在：

- [问题与评价模块发布记录](releases/foundations-rollout-2026-09-21.md)
- [学习、表示与泛化模块发布记录](releases/learning-generalization-2026-09-21.md)
- [预训练模型复用模块发布记录](releases/pretrained-reuse-2026-09-21.md)
- [模型、工具与人协作模块发布记录](releases/workflow-collaboration-2026-09-21.md)
- [反馈、运行与迁移模块发布记录](releases/feedback-transfer-2026-09-21.md)

旧课含义和旧实验保留在[教学基线 84aa171](https://github.com/h-lu/machine-learning-course/blob/84aa1710803f006cf781590126cafcd494bf5515/machine-learning-course/COURSE_MAP.md)，仅用于追溯，不能与新课包按相同编号任意拼接。

## 一次课怎样进入生产

完整课包必须同时具备学生 README、LEARN、SUPPORT、数据、配置、程序与报告提示，教师 RUNBOOK、REFERENCE、五个知识点和十道 A/B 题，以及与该版本对应的题库汇集文件。必须实际运行起点、支持配置、个人单因素修改、提交检查和确定性重跑。

结构检查只证明目录、链接和格式；专项测试证明它覆盖的计算；模型模拟冷读检查页面顺序和操作停顿；真人试读才支持“初学者可以独立完成”或“90 分钟可完成”的判断。发布记录必须将这些证据分开，未做的真人试读如实写明。

学生至少完成一个会影响实际结果或结论的选择。入门支持是完成同一必做任务的可执行路径，不是更低分的另一份作业。最后评价只在方案和评价规则固定后使用；已经看过的记录改称开发证据。

## 整仓验证

在完整工作区根目录运行：

```bash
python3 tools/validate_course.py
PYTHONPATH=ml-check pytest -q ml-check/tests
```

统一验证先从教师源文件生成 `ml-check/app/question_bank/lessons.json`，再运行学生实验、教师参考独立核算、检查器/API 测试和三个材料目录的严格检查。最终报告写入 `validation/latest.json`；它记录作者本机环境，不等同于真人课堂试教。

题库同步后必须人工核对：

- 仍有 32 个课次，每课五个知识点、A/B 各五题；
- A/B 同编号检查同一知识点，B 使用不同情境；
- AI 学习页只有知识点和学习说明，不显示题干或答案；
- 第 01–04 课的已结束场次仍读取历史题目快照；
- 第 05–32 课标题、材料和概念题对应同一目标；
- 健康接口报告本轮题库版本和 32 课。

## 历史场次与题库切换

服务从本轮起在创建场次时保存该课完整题目快照和题库版本。查看历史统计、学生反馈或解释时读取场次快照，不再按当前题库重新解释。部署前先用旧镜像中的题库为现有场次回填快照，再切换镜像。

原位更新前依次执行：

1. 确认当前场次已经进入 `result` 或 `closed`，没有 A、学习或 B 阶段正在进行。
2. 备份 SQLite 主文件及 WAL/SHM、旧题库、compose 配置和当前镜像标识。
3. 对备份运行 `PRAGMA integrity_check`，记录用户、场次、作答和学习完成数量。
4. 使用 `deploy/backfill_session_snapshots.py` 和正在生产使用的旧题库回填旧场次。
5. 构建新镜像，在本机或隔离端口检查登录、建场次、A/学习/B/结果、历史场次和 CSV 导出。
6. 切换生产容器，回读健康接口、题库版本、数据库数量和旧场次题目。
7. 保留旧镜像、旧题库和数据库备份，直到至少完成一次新课场次。

数据库备份和学生记录不提交到仓库。恢复时同时恢复旧镜像、题库与数据库，不能只换其中一项。

## 仓库同步

完整 GitHub 仓库保存课程规划、学生材料、教师材料、服务源码和验证记录。Gitea 的 `course-student-template`、`course-instructor`、`ml-check` 各自保留独立提交历史；学生发布仓库 `course-student-release-2026` 只同步学生可见文件，不包含教师答案和嵌套仓库信息。

推送后分别克隆远程仓库核对提交和关键文件，不能只凭本地 `git push` 退出码判断同步完成。生产部署按 [ml-check 部署说明](../ml-check/deploy/README.md)执行，并在模块记录中补充实际提交、镜像标识、备份路径、健康检查和部署时间。
