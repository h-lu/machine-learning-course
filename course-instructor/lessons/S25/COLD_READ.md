# S25 冷读与实际运行记录

## 按学生顺序得到的答案

- 第一次动作：在仓库根目录运行 `python scripts/course.py start 27`，随后运行起点配置。
- 动作前需要理解：一次选择、已观察反馈、未观察结果；README 首屏与 LEARN 的四轮表已在命令前解释。
- 命令读取 `lesson-27/data/base.json` 和 `config-start.json`，写入 `artifacts/start`。成功后先看 `rounds.csv` 中 seed=3、policy=epsilon 的 `chosen_response`、`observed_feedback`、`other_response_feedback`。
- 手算对象：一个策略、一个种子的 16 轮；反馈总和除以 16，单位为每轮平均反馈；回答次数之和也为 16。
- 单因素比较：探索率 0.10 与 0.25；个人检查改为 0.40，种子和反馈表不变。
- 失败处理：保留命令与完整报错，用四轮表继续区分未观察与 0，修复后补跑。

## 实际核对

支持配置中贪心策略平均反馈 0.50、平均覆盖 1 种回答；探索策略平均反馈 0.525、平均覆盖 1.8 种，平均简短/带步骤次数为 12/4。输出未泄露未选回答反馈。所有文档数字均可由 CSV 重算。

## 验证环境与性质

- 日期：2026-09-21；Linux x86_64；Python 3.14.4。
- 本机没有 `python` 命令别名；按学生通用操作指南将文档中的 `python` 换为 `/usr/bin/python3`，其余参数和顺序不变。
- 学生仓库基线提交：`d72bfd8e1a8106b1086e69c2312c5ea07b857067`，另含本次尚未提交的候选课包。
- 在临时复制 `/tmp/operations-cold-read-4ngkf1pn/course-student-template` 中操作，未读取教师答案后再执行学生命令。
- 这是模型模拟冷读和命令实际运行，不是真人学生试读，也不能证明 90 分钟时长。
- 六课共执行 34 个开始、起点、支持、个人检查、最终运行、提交检查及整仓重跑步骤；全部退出码为 0。`python scripts/course.py ci` 能重新生成六课确定性提交产物。


## 2026-09-21 独立复审补充

独立复审再次从 README 首屏进入 SUPPORT，运行起点、支持配置和探索率 0.40。`episodes.csv` 的嵌套计数已写为标准 JSON；五个种子全部保留，未选反馈未出现在策略输出。

本轮在临时复制 `/tmp/s25-s30-cold-final-CeY071/student` 中完成，不读取教师参考后再执行学生路径；六课的开始、起点、支持、个人设置、最终运行、提交检查和整仓 CI 均通过。这仍是模型模拟冷读与命令实跑，不是真人学生试读，也不能单独证明 90 分钟可完成。
