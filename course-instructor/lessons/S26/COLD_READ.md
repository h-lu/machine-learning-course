# S26 冷读与实际运行记录

## 按学生顺序得到的答案

- 第一次动作：运行第 28 课起点，打开 Q02 的两条轨迹。
- 动作前需要理解：状态、动作、即时分、延迟后果；字段在 README 和 LEARN 首次出现时解释。
- 命令读取相同服务台基础数据与起点配置，写 `trajectories.csv` 和 `summary.json`；先看 `id=Q02`、`policy`、`action`、`long_term_score`。
- 手算单位为本课定义的分：自动回答 `1-4=-3`，人工处理 `1-0.5=0.5`。总体分母为 8 条请求。
- 单因素比较：延迟伤害权重 1 与 4；个人检查删除可见状态中的 risk，其他项保持不变。
- 失败时可只用 Q02 小表计算，记录未运行并在修复后补跑。

## 实际核对

速度分选择 fast_auto（8 对 6）；长期分选择 risk_aware（-14 对 4）。删除 risk 后程序仍能运行，并增加不确定条件下的人工处理。材料没有要求学生实现尚未学习的 Q 学习。

## 验证环境与性质

- 日期：2026-09-21；Linux x86_64；Python 3.14.4。
- 本机没有 `python` 命令别名；按学生通用操作指南将文档中的 `python` 换为 `/usr/bin/python3`，其余参数和顺序不变。
- 学生仓库基线提交：`d72bfd8e1a8106b1086e69c2312c5ea07b857067`，另含本次尚未提交的候选课包。
- 在临时复制 `/tmp/operations-cold-read-4ngkf1pn/course-student-template` 中操作，未读取教师答案后再执行学生命令。
- 这是模型模拟冷读和命令实际运行，不是真人学生试读，也不能证明 90 分钟时长。
- 六课共执行 34 个开始、起点、支持、个人检查、最终运行、提交检查及整仓重跑步骤；全部退出码为 0。`python scripts/course.py ci` 能重新生成六课确定性提交产物。


## 2026-09-21 独立复审补充

独立复审使用删除 `risk` 的个人配置。输出显示 `risk_aware` 把未知风险请求转人工；材料已补充说明 `queue` 虽记录但给定策略未使用，避免把“可见字段”误写成“已参与决策”。标准术语改为“延迟奖励与回报”和“奖励函数设定不当”。

本轮在临时复制 `/tmp/s25-s30-cold-final-CeY071/student` 中完成，不读取教师参考后再执行学生路径；六课的开始、起点、支持、个人设置、最终运行、提交检查和整仓 CI 均通过。这仍是模型模拟冷读与命令实跑，不是真人学生试读，也不能单独证明 90 分钟可完成。
