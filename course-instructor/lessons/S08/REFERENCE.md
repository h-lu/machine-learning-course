# S08 教师参考：概率与行动

## 独立核算

```bash
python3 course-student-template/lesson-10/analysis.py --config course-student-template/lesson-10/config-start.json --output /tmp/S08-reference
```

验证集 12 条、正类 4 条；`[0.4,0.7)` 有 3 条、平均概率约 0.602、实际比例 1/3。阈值 0.5 选 5 条，精确率 0.8、召回率 1；阈值 0.4 选 6 条，精确率约 0.667、召回率仍为 1。C28 是新增误报。

## 两条合理路线

若人工容量紧且漏报在当前样本为 0，可保留 0.5。若业务明确更重视漏报，可选更低阈值，但必须承认当前验证集未显示召回改善，并记录容量。固定前 k 名适用于每天名额稳定的情境。

## 反例与评价

降低阈值在总体逻辑上不会减少被选记录，但小批数据中召回率可保持不变。单条 0.8 最终标签为 0 不单独证明失准。评价分组分母、错误数、容量与测试前规则，不按默认阈值给分。

## 限度

概率由教学公式保存；本课验证评价流程，不验证真实模型校准。
