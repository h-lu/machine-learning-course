# S14 教学运行单 · 输入怎样影响模型能够利用的信息

状态：模块候选课包，未部署。学生目录为 `lesson-16`。

## 主要关系与前置知识

学生要通过可查看中间数值的微型实验说明：因果遮罩限制信息路径，改变未来值不应影响较早位置。前置仅需 S13 的输入与参数区别和基本加权平均；softmax 在课内手算。不得用黑箱回答差异冒充内部遮罩证据。

## 课前准备与最小资源

```bash
python3 course-student-template/lesson-16/analysis.py --config course-student-template/lesson-16/config-start.json --output /tmp/S14-start
python3 course-student-template/lesson-16/analysis.py --config course-student-template/lesson-16/config-support.json --output /tmp/S14-compare
```

核对 sequence-01 因果输出约 1.668，未来值 5→8 时变化为 0；无遮罩输出约 3.419→4.662。人工键和值不是真实词元表示，脚本不训练参数。

## 90 分钟组织

0–18 分钟画位置和可见集合；18–30 分钟运行；30–48 分钟手算 exp 分母、权重和输出；48–60 分钟只改变未来值；60–72 分钟联系 S13 并写机制边界；72–90 分钟概念检查与提交。

巡视时要求学生先指出目标位置 1、未来位置 3，再读表。若只说“0 没变化”，追问 0 的对象和条件。若把输出叫正确率，回到数据说明说明它是无单位加权值。

## 必做成果与反馈

一项完整手算；有无遮罩的前后比较；个人未来值和按权重预计的改变量；S13 与 S14 证据关系；真实模型不能推出的结论。提高任务才改变键或扩展向量，不把这些概念提前设为必做。

失败支持：Python 不可用时先用给定分数手算；程序拒绝配置时检查未来位置是否真的大于目标位置。学生修改键和值两项时，引导拆成两次运行。

## 概念检查

五点为查询键值、softmax 加权、因果遮罩、机制与行为证据、概率与事实。数值题可以独立手算；B 题换到时间序列与在线模型。
