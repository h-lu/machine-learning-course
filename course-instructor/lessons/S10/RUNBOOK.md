# S10 教学运行单：目标函数与一次参数更新

状态：已随 `ml-v13-course-map-2026-09-21` 于 2026-09-21 发布；本次没有真人 90 分钟试读。学生目录为 `lesson-12`。

## 主要关系与前置知识

本课把 S09 的“模型看到的数值”推进到“参数怎样因目标函数而更新”。只要求追踪微型逻辑回归的一次整批梯度更新和学习曲线；不推导完整微积分，也不引入借来表示。

## 课前准备与最小资源

运行 `python3 course-student-template/lesson-12/analysis.py --output /tmp/S10-check`，确认 `first_update.csv`、两条学习曲线和逐条概率。核对消息长度参数：`0 - 0.15×(-0.33004529)≈0.04950679`。程序独立运行，不读取学生 S09 文件，课堂上必须说明来源边界。

## 90 分钟组织

0–20 分钟沿预测、损失、梯度、更新追踪一次；20–35 分钟学生运行和手算；35–55 分钟比较学习率；55–65 分钟个人单因素实验；65–75 分钟测试前选择；75–90 分钟概念检查。

## 巡视问题与完成证据

询问：“负梯度为什么使参数增加？”“准确率相同时损失为何不同？”“损失下降还不能说明什么？”最低证据是一次更新重算、曲线比较、只改一项设置和结论边界。

## 失败支持与分层

不会读曲线时只看第 0、1、10、最后一轮。数值溢出由程序裁剪处理；学生不需要改算法。提前完成者寻找开始不稳定的学习率并记录完整曲线。

## 概念检查

五点为参数、目标函数、梯度方向、学习率、优化与用途的区别。
