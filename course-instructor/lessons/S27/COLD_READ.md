# S27 冷读与实际运行记录

## 按学生顺序得到的答案

- 第一次动作：运行 v1 起点并在 current 批次找到 M09、M11。
- 动作前需要理解：参考批次、当前批次、版本、延迟标签；README 与 DATA 在第一次筛选前解释。
- 命令读取固定预测日志，不训练模型；生成 `records.csv`、`batch_metrics.csv` 和 `summary.json`。先看 `label_available` 与 `known_correct`。
- 手算分母：当前 6 条，其中 4 条已有标签、2 条未知；v2 已有标签正确率为 2/4，不是 2/6。
- 单因素比较：起点候选 v1 与支持候选 v2；个人只把 `known_accuracy_minimum` 从 0.75 改为 0.40。
- 失败时保留固定日志和报错，先用四条已有标签重算，不能猜两条未知。

## 实际核对

v2 的新条件为 4/6、低置信 2/6、已有标签正确 2/4；标签前与标签后报警均触发。v1 恢复检查在已有标签上为 4/4，输出仍明确两条未知，未写成整个当前批次 100%。

## 验证环境与性质

- 日期：2026-09-21；Linux x86_64；Python 3.14.4。
- 本机没有 `python` 命令别名；按学生通用操作指南将文档中的 `python` 换为 `/usr/bin/python3`，其余参数和顺序不变。
- 学生仓库基线提交：`d72bfd8e1a8106b1086e69c2312c5ea07b857067`，另含本次尚未提交的候选课包。
- 在临时复制 `/tmp/operations-cold-read-4ngkf1pn/course-student-template` 中操作，未读取教师答案后再执行学生命令。
- 这是模型模拟冷读和命令实际运行，不是真人学生试读，也不能证明 90 分钟时长。
- 六课共执行 34 个开始、起点、支持、个人检查、最终运行、提交检查及整仓重跑步骤；全部退出码为 0。`python scripts/course.py ci` 能重新生成六课确定性提交产物。


## 2026-09-21 独立复审补充

独立复审把 `known_accuracy_minimum` 从 0.75 改为 0.40；标签后报警由真变假，而标签前报警仍为真。两条未知记录始终未进入正确率分母，恢复版本只报告已有标签的 4/4。

本轮在临时复制 `/tmp/s25-s30-cold-final-CeY071/student` 中完成，不读取教师参考后再执行学生路径；六课的开始、起点、支持、个人设置、最终运行、提交检查和整仓 CI 均通过。这仍是模型模拟冷读与命令实跑，不是真人学生试读，也不能单独证明 90 分钟可完成。
