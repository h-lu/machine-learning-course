# S17 教学运行单 · 该改提示、补上下文，还是改参数

状态：模块候选课包，未部署。学生目录为 `lesson-19`。

## 主要关系与前置知识

主要关系是适配位置应与失败原因、数据和维护条件对应。S15、S16 已提供提示与检索证据；参数适配在本课仅有固定候选材料，没有训练过程。学生必须区分“比较已有输出”和“重现训练”。

## 课前准备与最小资源

```bash
python3 course-student-template/lesson-19/analysis.py --config course-student-template/lesson-19/config-start.json --output /tmp/S17-context
python3 course-student-template/lesson-19/analysis.py --config course-student-template/lesson-19/config-support.json --output /tmp/S17-parameter
```

提示/上下文任务率分别 4/6、6/6，资料支持 2/6、6/6；参数候选任务率 5/6，资料支持 1/6。准备分钟数为课程估计，不是实测运行时间。

## 90 分钟组织

0–15 分钟画三条路线改动位置；15–28 分钟第一组比较；28–42 分钟逐案例检查；42–55 分钟第二组比较；55–70 分钟按用途和约束选择；70–75 分钟连到前课；75–90 分钟概念检查与提交。

巡视时优先检查学生是否在相同 6 条案例上比较，是否把“训练样例”误当事实来源。问清预算或更新频率如何改变建议，不把最高比例设为唯一结论。

## 必做成果与反馈

两组路线表、至少两个案例、参数更新范围、数据/资料/时间条件、学生自己的两路线选择、S15/S16 文件引用。采用上下文、采用低成本提示、组合或暂缓参数适配均可接受。

新增案例没有三条新输出时，允许并要求报告无结果。不得静默套用固定缓存。实现困难不要求临时训练大模型。

## 概念检查

五点为提示、上下文、参数、相同评价、质量与成本。B 题换到摘要、价格和预算情境。
