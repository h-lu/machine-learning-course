# S29 冷读与实际运行记录

## 按学生顺序得到的答案

- 第一次动作：先写图书馆使用者、输入、输出和人工处理条件，再运行两个方案。
- 动作前需要理解：场景迁移、公平比较和未覆盖条件；不需要先懂新算法。
- 命令读取 8 条 development 图书馆请求，生成 `records.csv` 与 `summary.json`；先看 T01、T02 三列路线。
- 手算单位为请求：原方案 6/8，适配方案 8/8；两个方案分母一致。
- 条件变化：在 `config.json` 的 `added_request` 中新增英语续借请求 T09，先给参考路线理由再运行。程序没有覆盖英语，失败或转人工均需保留解释。
- 失败时不能改参考路线迎合输出；保存新请求和实际路线。

## 实际核对

默认差异集中在 T01、T02；个人信息与预约仍转人工。新增 T09 的命令成功；`provided_comparison` 保留原 8 条的 6/8 与 8/8，`comparison` 自动把分母增为 9，得到 6/9 与 8/9。材料明确旧设备分数不能代替图书馆重测，单条英语检查也不能代表英语总体。

## 验证环境与性质

- 日期：2026-09-21；Linux x86_64；Python 3.14.4。
- 本机没有 `python` 命令别名；按学生通用操作指南将文档中的 `python` 换为 `/usr/bin/python3`，其余参数和顺序不变。
- 学生仓库基线提交：`d72bfd8e1a8106b1086e69c2312c5ea07b857067`，另含本次尚未提交的候选课包。
- 在临时复制 `/tmp/operations-cold-read-4ngkf1pn/course-student-template` 中操作，未读取教师答案后再执行学生命令。
- 这是模型模拟冷读和命令实际运行，不是真人学生试读，也不能证明 90 分钟时长。
- 六课共执行 34 个开始、起点、支持、个人检查、最终运行、提交检查及整仓重跑步骤；全部退出码为 0。`python scripts/course.py ci` 能重新生成六课确定性提交产物。


## 2026-09-21 独立复审补充

独立复审先完成 8 条共同分母比较，再用配置加入一条英语续借请求。两方案都把它转人工，失败被保留；原 8 条与加入第 9 条后的分母在 summary 中分开保存。

本轮在临时复制 `/tmp/s25-s30-cold-final-CeY071/student` 中完成，不读取教师参考后再执行学生路径；六课的开始、起点、支持、个人设置、最终运行、提交检查和整仓 CI 均通过。这仍是模型模拟冷读与命令实跑，不是真人学生试读，也不能单独证明 90 分钟可完成。
