# S09 教学运行单：表示、尺度与特殊输入

状态：已随 `ml-v13-course-map-2026-09-21` 于 2026-09-21 发布；本次没有真人 90 分钟试读。学生目录为 `lesson-11`。

## 主要关系与前置知识

本课固定 3 近邻方法，只改变记录表示，以便学生观察表示怎样改变距离、近邻与预测。前置只需训练集参数和分类准确率；不引入梯度或预训练。

## 课前准备与最小资源

运行 `python3 course-student-template/lesson-11/analysis.py --output /tmp/S09-check`。确认 `raw` 与 `prepared` 结果、P01–P03 三条挑战记录和训练统计量。教师应能手算 `sqrt(20²+2²)`，并说明缩放参数只能由训练集计算。

## 90 分钟组织

0–15 分钟用两维距离演示尺度；15–30 分钟运行并找 C28；30–50 分钟比较 raw/scaled/prepared；50–65 分钟检查缺失、新类别、少见正常；65–75 分钟测试前选择；75–90 分钟概念检查。

## 巡视问题与完成证据

询问：“哪项字段主导原始距离？”“均值来自哪一组？”“P02 为什么不能静默当作 web？”最低证据是一个缩放手算、同方法表示比较、至少两条挑战记录和个人选择。准确率不提高也能形成合格结论。

## 失败支持与分层

若学生迷失在全部列，先只看 C28 的 `nearest_ids`。缺失值不等于 0，类别编号也不应当作自然距离。提前完成者拆分 scaled 与 prepared，判断是哪组处理改变结果。

## 概念检查

五点为尺度与距离、训练集统计量、缺失表示、未见类别、相似不等于同类。
