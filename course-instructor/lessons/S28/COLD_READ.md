# S28 冷读与实际运行记录

## 按学生顺序得到的答案

- 第一次动作：运行三次计时起点，先打开确定性 `summary.json`，再读原始 `timings.csv`。
- 动作前需要理解：质量、延迟、估算成本、缓存和重复测量；文档明确本地哈希不是模型推理。
- 命令读取 10 条请求和配置，生成 `summary.json`、`records.csv`、`timings.csv`；先核对 fast 8/10 与 thorough 10/10。
- 手算：fast 开缓存时 8 次新计算乘单价 1 等于 8 个估算单位；质量分母始终为 10 条，不乘计时次数。
- 单因素比较：计时重复 3 次与 5 次；个人在 `config.json` 的 `repeat_input_check` 中只替换 P08 文字，实际检查缓存命中变化。
- 失败或计时异常时保留环境和原始计时，不要求毫秒与教师机器相同。

## 实际核对

最终独立运行中，fast 无缓存五次平均每条延迟为约 0.0834–0.0919 毫秒/条，thorough 为约 0.5111–0.5505 毫秒/条；这是本次机器实测，其他机器无需相同。基础请求有 2 次缓存命中；只替换 P08 后，两套开启缓存的方案均降为 1 次命中。`summary.json`、`records.csv` 与 `changed_records.csv` 不含实测耗时，整仓逐字节重跑通过；`timings.csv` 单独保存本次实测，不进入逐字节比较。另在临时 Git 仓库实际执行 `git add -f lesson-30/artifacts/timings.csv`，确认该文件能够暂存。

## 验证环境与性质

- 日期：2026-09-21；Linux x86_64；Python 3.14.4。
- 本机没有 `python` 命令别名；按学生通用操作指南将文档中的 `python` 换为 `/usr/bin/python3`，其余参数和顺序不变。
- 学生仓库基线提交：`d72bfd8e1a8106b1086e69c2312c5ea07b857067`，另含本次尚未提交的候选课包。
- 在临时复制 `/tmp/operations-cold-read-4ngkf1pn/course-student-template` 中操作，未读取教师答案后再执行学生命令。
- 这是模型模拟冷读和命令实际运行，不是真人学生试读，也不能证明 90 分钟时长。
- 六课共执行 34 个开始、起点、支持、个人检查、最终运行、提交检查及整仓重跑步骤；全部退出码为 0。`python scripts/course.py ci` 能重新生成六课确定性提交产物。

## 2026-09-21 独立复审补充

独立复审确认实测字段只存在于 `timings.csv`；两个确定性记录文件没有任何耗时列。最终个人配置把 P08 改为新的同路线文字，缓存命中从 2 降到 1，成本估算随新计算次数改变。

本轮在临时复制 `/tmp/s25-s30-cold-final-CeY071/student` 中完成，不读取教师参考后再执行学生路径；六课的开始、起点、支持、个人设置、最终运行、提交检查和整仓 CI 均通过。这仍是模型模拟冷读与命令实跑，不是真人学生试读，也不能单独证明 90 分钟可完成。
