# 第 21–26 课模型、工具与人协作模块发布记录：2026-09-21

## 范围与版本边界

本记录适用于学生 `lesson-21` 至 `lesson-26`，对应 S19–S24。第 01–04 课已授课并冻结，本模块不改变历史学生要求或旧场次解释。

六课复用一个设备借用服务流程，依次加入分步处理、本地工具、受限动态流程、权限边界、步骤故障定位和端到端改进。所有动作都发生在无真实副作用的离线环境。最终汇集题库目标版本为 `ml-v13-course-map-2026-09-21`。

| 课次 | 标题 | 主要可复核证据 |
|---|---|---|
| S19／第 21 课 | 怎样把任务拆成模型、程序和人的协作 | 一步基线与分步流程、完整中间轨迹及人工交接 |
| S20／第 22 课 | 什么时候应调用工具，而不是继续生成 | 工具参数、返回状态和值，以及无效参数的失败结果 |
| S21／第 23 课 | 固定流程还是由模型选择下一步 | 统一动作单位、工具失败、有限重试、停止条件与动作预算 |
| S22／第 24 课 | 输入里的指令可以改变系统职责吗 | 文档原文、匹配短语、候选动作、权限和实际动作日志 |
| S23／第 25 课 | 多步方案失败，应该先改哪一步 | 原流程、故障和替换条件下的完整局部值与端到端结果 |
| S24／第 26 课 | 怎样用下一项实验改进整件作品 | 开发比较、运行前固定的单项候选和互斥 holdout 最后评价 |

## 学生路径与产物

[S19–S24 冷读与运行记录](../reviews/s19-s24-cold-read-2026-09-21.md)只按学生页面前进，并在普通 CPU 上实际执行六课各自的 start/support 命令、个人条件和提交收尾。12 条 start/support 命令及一条 S24 final 命令均返回 0，生成 `summary.json` 与相应 CSV；每课自拟条件都实际改变了轨迹或结论。

S19–S23 在一次性学生副本中依次完成 `course.py start`、采用配置、`course.py run`、状态改为 `complete` 和 `course.py check`。S24 先运行 development 并确认默认提交不读取 holdout，再显式切换到 final 提交清单；两阶段及最终检查均通过。

S24 的 W01–W12 是开发数据，F01–F06 单独位于 `data/holdout.json`。默认 `scripts/course.py run 26` 只读取 `base.json + config.json` 并生成开发结果；学生固定候选后才显式使用 `submission-final.json`、`config-final.json` 和 holdout。公开仓库中的 holdout 仍依赖操作顺序，不能当作高风险评价中的保密数据。

## 已有验收证据

- `course-student-template/tests/test_workflow_module.py` 以 unittest 和 pytest 两种入口运行，均为 17 项通过。
- `course-instructor/scripts/validate_reference_coverage.py --lesson S24` 通过；它显式读取 holdout，两次产生非空 `final_cases.csv` 且逐字节一致。
- 专项测试覆盖共用开发数据、透明一步基线、工具无效值、失败/重试/停止/阈值/人工交接、文档编号校验、步骤局部字段、开发与 holdout 编号互斥、模式错配拒绝、单因素限制及真实命令行产物。
- 源题已经按实际动作数和 S24 的开发/最后评价分离修订；最终 v13 汇集与整库服务测试仍由发布前统一验证确认。

## 证据边界

模拟冷读和实际命令是两类证据：前者检查页面顺序和停顿点，后者证明程序及产物可运行。没有真人学生首次试读，也没有 90 分钟课堂计时；这些结果不能证明所有学生能独立完成或正确解释模型、程序、工具和人工交接各自的职责。

## 发布后补记

- GitHub 完整仓库提交：待补。
- Gitea 学生模板、教师仓库、`ml-check` 和学生发布仓库提交：待补。
- v13 题库 SHA-256、整仓验证和完整服务测试结果：待补。
- 生产镜像、部署时间、数据库与旧题库备份、健康接口和旧场次快照核对：待补。
- 真人试读：尚未进行；后续应记录步骤追踪、手算、个人条件和报告解释的实际完成情况。
