# 第 15–20 课预训练模型复用模块发布记录：2026-09-21

## 范围与版本边界

本记录适用于学生 `lesson-15` 至 `lesson-20`，对应 S13–S18。第 01–04 课已授课并冻结，本模块不会改变其学生要求、题目解释或成绩依据。

六课共用“离线课程问答助手”情境，依次区分能力来源、输入信息路径、提示比较、检索增强、适配路线和开放结果评价。实验使用人工构造的小数据、规则生成器、微型机制或固定候选；材料明确这些证据不能冒充真实预训练大模型的新能力。汇集题库已经以 `ml-v13-course-map-2026-09-21` 发布。

| 课次 | 标题 | 实际核对的对象 |
|---|---|---|
| S13／第 15 课 | 预训练模型的能力来自哪里 | 三条能力变化路线、参数更新范围和能力边界 |
| S14／第 16 课 | 输入怎样影响模型能够利用的信息 | 注意力权重、因果遮罩及改变未来值对较早输出的影响 |
| S15／第 17 课 | 提示改进了，还是只适配了见过的例子 | 开发问题上的重复响应、匹配新提示的新输出及锁定后的 final |
| S16／第 18 课 | 怎样补充资料，而不是期待模型猜对 | 检索结果、回答支持、失败阶段和无法回答请求 |
| S17／第 19 课 | 该改提示、补上下文，还是改参数 | 相同案例上的两条固定适配路线及其改变环节 |
| S18／第 20 课 | 没有唯一答案时，怎样判断真的变好了 | 四维评价、两位评分、分歧、顺序偏差代理和独立 final |

## 学生路径与产物

[模块冷读记录](../reviews/s13-s18-pretrained-module-cold-read-2026-09-21.md)按 README → SUPPORT → 所需 LEARN → 实际命令 → 指定 CSV 行 → 个人单因素配置 → 必要时 final → 报告与提交检查的顺序执行。全新副本演练覆盖六课的 start、support、personal、必要时 final、默认产物、提交文件检查和默认入口重跑，均成功；六个默认入口各约 0.02 秒，这只是程序运行耗时。

本轮还修正了 Windows 学生复制配置的替代说明、final 配置必须带入已采用阈值、命令执行目录、常见 JSON 错误提示，以及输出中绝对路径造成的新副本哈希差异。

## 已有验收证据

- 模块记录中的学生和教师严格材料检查均为 32 课、0 项需处理。
- `course-student-template/tests/test_pretrained_module.py` 在该次模块验收中为 8 项通过，覆盖单字段配置、参数更新范围、因果遮罩、提示新输出、检索与回答失败、未知固定路线拒绝、评价顺序和新副本确定性重建。
- 每课五个知识点和 A/B 各五题已逐对人工阅读；同编号检查同一关系，B 使用不同情境，学习说明没有复述题目情境。模块记录中的 S14、S15、S18 数值答案已经独立重算。
- 模块记录写成时，共享旧运行测试和教师参考尚未全部迁移，因此当时的全量旧测试存在失败，不能把那次结果写成整仓通过。并行修改完成后的 `tools/validate_course.py` 和完整 `ml-check` 测试已经重新运行并通过，数量见文末正式发布记录。
- 模块开发记录曾使用 `ml-v10-pretrained-2026-09-21` 标识源题；生产发布以最终汇集的 v13 文件、摘要和历史场次快照为准，不用旧开发标识解释线上状态。

## 证据边界

冷读由模型模拟，没有真人参与，也没有课堂计时。命令运行证明页面、字段和数字能接起来，不能证明学生能在 90 分钟内完成，也不能证明学生不会把人工注意力值、固定响应、规则生成器或模拟评分器误解为真实大模型能力。真人试读必须把这类误解作为单独观察项。

## 正式发布记录

- 内容提交：GitHub 完整仓库 `fea7ab2`；Gitea 学生模板 `f0807a4`；教师课包内容 `19a1d2a`；`ml-check` `e7bf5a0`；学生发布仓库 `4f7406f`。
- 题库：`ml-v13-course-map-2026-09-21`，SHA-256 为 `537bf31972b8cb3655f1cd1e9fcd245ea7bf6713ae3187d899e4c4e446c9720f`。
- 整仓验证：`python3 tools/validate_course.py` 的 8 组检查全部通过，其中学生实验 131 项、32 课教师参考 117 项独立核算、教师脚本 15 项、`ml-check` 单元测试 81 项通过，学生、教师和课程规划严格检查均通过；另行运行 `PYTHONPATH=ml-check pytest -q ml-check/tests`，81 项通过。
- 学生路径实跑：在全新学生仓库副本中完成第 05–32 课 151 条命令，全部退出码为 0；随后执行整仓 `python3 scripts/course.py ci` 通过。该结果证明命令和确定性产物可以衔接，不等于真人理解或课堂用时证据。
- 生产发布：镜像 `ml-check:2026-09-21-v13` 于 `2026-09-21T18:33:59Z` 至 `2026-09-21T18:34:03Z` 完成切换；内网和公网健康接口均返回正常。
- 回退材料：数据库备份为 `/home/ubuntu/ml-check/data/ml-check.before-v13-20260921T182552Z.sqlite3`，服务源码与部署文件备份为 `/home/ubuntu/ml-check/backups/source-before-v13-20260921T182552Z.tar.gz`；历史题库文件保存在 `/home/ubuntu/ml-check/data/session-banks/`。
- 历史数据核对：切换前后用户、场次、作答和学习完成记录分别为 64、4、1649、169，SQLite 完整性检查通过；场次 7、8、9、11 分别固定到 v4、v6、v11、v11 题目快照，不随 v13 改写。
- 真人试读边界：本次没有真人首次试读或 90 分钟课堂计时。后续课堂观察需单独记录学生停顿点、概念解释和各阶段实际用时，不能用上述模拟冷读与命令实跑替代。
