# S05 实验报告

完成实验后，用实际数据说明：

把下面提示替换成自己的回答，每项几句话即可。程序结果来自实际运行，不手填一个期望的分数。

## 1. 初始训练数据缺少什么？为什么准备优先补这种样本？

在这里写回答，并指向对应配置、结果文件和运行命令。

## 2. 相同预算下，我比较哪些补充办法？各选中哪些编号与时段？

在这里写回答，并指向对应配置、结果文件和运行命令。

## 3. 新增标签来自候选池还是模型生成？有没有偷看评价标签来挑样本？

在这里写回答，并指向对应配置、结果文件和运行命令。

## 4. 在同一验证集上，总体与分组误差怎样改变？换一次种子或预算后呢？

在这里写回答，并指向对应配置、结果文件和运行命令。

## 5. 哪种失败仍未覆盖？为什么更多合成样本不等于新的真实证据？

在这里写回答，并指向对应配置、结果文件和运行命令。
