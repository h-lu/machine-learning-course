# S03 实验报告

完成实验后，用实际数据说明：

把下面提示替换成自己的回答，每项几句话即可。程序结果来自实际运行，不手填一个期望的分数。

## 1. 两个可能的使用问题与预计

分别说明“预测已有窗口的未来日期”和“预测训练中没有出现的新窗口”这两个问题。对每个问题写下你预计更接近 time 还是 group，以及理由。选出本次主要准备支持的一个用途，并在看结果前写明：出现什么结果时，你会限制或暂停原建议。

## 2. 训练、验证和保留测试的证据

完成下面两行。编号较多时可以写编号范围，但必须能看出窗口与日期；同时说明第 7–8 天的测试编号是否进入开发比较。

| 使用问题 | 对应划分 | 训练编号与 `train_n` | 验证编号与 `n` | `shared_sites` | MAE/分钟 | 没有覆盖的情况 |
|---|---|---|---|---:|---:|---|
| 已有窗口的未来日期 | 待填写 | 待填写 | 待填写 | 待填写 | 待填写 | 待填写 |
| 训练中没出现的新窗口 | 待填写 | 待填写 | 待填写 | 待填写 | 待填写 | 待填写 |

## 3. 一项 MAE 手算和一条泄漏反例

用 time 方案的六条验证记录手算一次 MAE：列出六个 `absolute_error`、分母和结果，并与 `comparison.csv` 核对。再选 B05 或其他一条记录，说明正常预测和泄漏示范分别使用什么信息，以及为什么泄漏示范不能采用。

## 4. 我的检查与选择理由

先抄下运行前写的改变条件，再填写实际检查。如果改日期，列出训练/验证编号、分母和 MAE 怎样变化，并说明为什么不同编号上的 MAE 不能直接写成性能升降；如果改种子，至少列出三个种子的编号与 MAE，并计算最小值、最大值和差值。最后写一条支持原建议的证据和一条削弱原建议的证据，指向对应配置、结果文件和运行命令。

## 5. 结论的范围和下一步

分别为“已有窗口的未来日期”和“新窗口”写一句建议，再说明你本次实际准备支持哪一种。把第 4 节的改变条件与实际结果对应起来，写出有证据的保留、限制或暂停理由。说明还有哪些时间、窗口或对象变化没有单独检查；不要把开发验证结果写成最终测试结论。
