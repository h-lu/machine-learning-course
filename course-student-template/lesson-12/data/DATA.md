# S10 数据说明

本课使用 48 条人工咨询复核记录：训练 24、验证 12、测试 12。一行代表一条咨询记录。

模型输入为消息长度（词）、历史记录数（条）、紧急词次数和附件是否缺失；标签 `needs_review` 为 1 或 0。程序只用训练集计算每个数值字段的均值和标准差，再对其他数据使用同一参数。

课程数据为确定性的机制练习。标签不来自真实人员，不能用于评价任何个人或部署模型。验证集比较学习率或轮数；测试集只评价事先确定的设置。
