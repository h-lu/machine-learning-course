# S12 数据说明

本课继续使用 48 条人工咨询复核记录：训练 24、验证 12、测试 12。一行是一条咨询，输入为消息长度、历史记录数、紧急词次数和附件是否缺失，标签是是否需要人工复核。

起点会从四个缩放字段构造平方项与交互项，共 10 个输入；简化候选只使用四个缩放字段。缩放参数只由训练集计算。

小验证集只有 12 条，一条分类变化会让准确率改变约 `1/12≈8.3` 个百分点。因此应同时看损失、逐条记录和前几课证据，不把一次小变化写成普遍规律。数据完全人工生成，不支持真实部署结论。
