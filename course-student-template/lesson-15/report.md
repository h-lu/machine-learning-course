# S13 实验报告：能力变化来自哪里

## 用途和预计

离线课程问答助手要判断什么？谁会使用这个结果？运行前，我预计固定记录、输入规则和训练输出头分别会改变什么；哪种结果会改变我的建议？

## 相同评价记录上的比较

三条路线各评价多少条记录？准确率和对数损失分别是多少？为什么不能只挑一个数宣布最好？

## 一条记录和一次参数核对

列出 `ev-02` 的标签及三种分数，说明阈值前后分类。输出头的 `bias`、`weight` 和训练损失从第 0 步到最后怎样变化？基础分数有没有更新证据？

## 我的单因素检查

我只改了哪个 `context_boost`，为什么？预计和实际分数是什么？哪些路线应保持不变？

## 建议、证据与限制

我建议保留、修改还是暂缓哪条路线？分别写出本实验能支持和不能支持的结论，并引用结果文件。六课连续证据的起点是哪一个 `evidence.json`？
