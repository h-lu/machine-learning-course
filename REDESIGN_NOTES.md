# 2026-09-06 课程重建

本轮按“归档当前内容、完全重新设计”处理。课程仍是 16 周、32 次课，保留经典学习、深度学习、Transformer、语言模型应用、强化学习及使用后的检查。重建目标是让学生能理解并完成一个有自主选择的小项目。

## 旧版为什么需要重建

旧版的学习卡大量共用同一套文字：先列术语，再让学生自己找几行数据，没有解释本课机制的具体例子。部分任务虽写“开放”，程序和检查却限定固定方案。

代码审查发现，27 课的起步程序只读取数据并统计行数；真正的参考实现和覆盖检查主要集中在 S05、S10、S15、S23、S28 五课。“32 课程序都能退出成功”因此不能证明 32 课已经具备实验内容。旧检查器还有通过搜索关键词确认开放性的做法。

原内容已移至 [归档](archives/2026-09-06-before-redesign/README.md)。完整快照含四仓 Git 历史，并校验了 2,439 个普通文件。旧文案生成脚本也在归档中，不再影响新内容。

## 新版怎样让选择有实质影响

每课的必做任务规定必要概念、主要比较与检查要求，学生可以改变问题、数据、方法、评价和使用建议。例如，S05 中有限复核名额与尽量少漏报可能支持不同策略；S03 中预测新用户与预测同一用户的未来需要不同评价安排。

学生可以更换程序入口、报告结构和结果文件，不用围绕大量固定 JSON 字段填写答案。教师参考按不同用途解释多条合理路线；有依据地选择规则或停止使用模型，都可以得到高评价。

学习卡逐课给出有数字的最小例子、必要概念、具体误区和判断题。每课有独立情境的 A/B 概念练习。AI 可以协助完成全部代码；学生需要选择用途、检查计算并解释结果带来的修订，不要求上交完整对话。

这部分借鉴了[统计课的设计标准](https://github.com/h-lu/statistics-course/blob/main/instructor-guide/COURSE_DESIGN_STANDARD.md)与[第 04 课任务](https://github.com/h-lu/statistics-course/blob/main/student-template/lesson-04/README.md)中把实质选择放入必做项目的做法。本次将其评分结构迁移到机器学习课：平时 50 分按课堂项目完成与代表作品质量分开记录；业务场景、课程内容和学生文案仍按本课需要编写。

## 实验与检查的作用

32 个起步实验分别计算本课内容。小模型使用 NumPy，数据随课提供；不依赖显卡、付费接口或课堂下载模型。神经网络更新、注意力、解码、检索和表格决策都有实际计算。

每个默认输出明确表示入门示例，既不填写最终采用建议，也不把学生清单改成完成。格式检查允许不同模型和指标；自动验证只核对结构、计算和路径。教学开放性与结论依据仍由人阅读。

固定响应只能评价已提供的候选。新提示没有真实输出时，不能将旧缓存称为新提示的实验；微型 Transformer 的一次输出头更新也不等于完成语言模型预训练。合成数据的结果不能直接用于真实业务。这些限制在对应学习材料和数据说明中写明。

## 目录与接口变化

| 内容 | 本轮处理 |
|---|---|
| 课次编号 | 保留 C01、C02、S01–S30，与总表一致 |
| 学生与教师位置 | 从 `ml-gitea-prototype/` 移到根下 `course-student-template/`、`course-instructor/`，保留各自 Git 历史 |
| 逐课文件名 | 保留 README、LEARN、analysis.py、contract.json、submission.json、update.md、artifacts/summary.json |
| 任务与提交字段 | 改为第 2 版：任务字段精简、提交入口与路径可替换；旧 JSON 字段并非全部兼容 |
| 检查命令 | 保留 `--repo`、`--profile`、`--expected-count`、`--strict`、`--json` |
| HTTP 路径 | 保留健康检查和课次读取路径；返回题目结构为第 2 版，学生响应不含答案 |
| 服务启动 | 本地读取服务继续使用标准库，启动为 `python3 -m app.main`；新增匿名 A/B 完成凭据，可用 SQLite 持久化，部署配置见 `ml-check/deploy/README.md` |
| 旧自动发布与文案生成 | 完整保存在归档中，新材料不使用旧五课限定流程 |

四个本地仓库的重建工作分支为 `codex/open-course-redesign`。重建完成后，课程作者要求将完整材料（包括教师参考与题目答案）公开到 GitHub，并更新 hblu.top 上的 Gitea 课程仓库。发布位置和验证记录见 [发布记录](PUBLISHING.md)。本机完整归档继续保留，公开发布包不包含旧版压缩快照或嵌套 Git 数据库。

## 验收记录

统一入口为 `python3 tools/validate_course.py`。它检查题库同步、学生测试、32 课教师参考覆盖、检查器与 API 测试，以及学生、教师、规划三个仓库的严格模式。结果见 [自动验收记录](validation/latest.json)；独立环境验证见 [干净环境重跑](validation/clean_environment.json)。

本轮最终结果：学生 19 项测试、检查器与 API 的 27 项测试全部通过；教师覆盖 32 个变体、48 项独立核算通过；三个材料仓库的严格检查均为零问题。128 道概念题已同步，学生可见旧术语扫描与本地链接检查通过。

独立环境使用 Python 3.14.4、NumPy 2.5.2。NumPy 从预先下载的安装包离线安装，没有借用系统依赖。复制源码时排除旧输出和缓存，32 个命令逐一重跑共约 1.95 秒，最慢约 0.12 秒，进程峰值内存最高约 43 MiB。这里测量的是作者本机上的微型起步实验，不包含环境安装、学生编程、阅读或分析时间。

可再次运行 `python3 tools/validate_clean_environment.py --python /path/to/venv/bin/python` 验证；指定环境须独立安装 NumPy。当前机器缺少 `ensurepip`，本轮通过 `venv --without-pip` 建立环境，再用已有 pip 的 `--python` 参数和本地安装包安装依赖。这项环境差异已记录，未安装系统软件包。

本机验证记录运行时间、环境和数值结果，不能代替普通学生电脑上的试教。正式开课前仍需观察学生能否在 90 分钟内读懂问题、作出实质选择、完成检查和解释。成绩权重、考试和正式发布版本由教师另行确定。
