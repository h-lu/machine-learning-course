# 机器学习课程内容编写规范

更新日期：2026-09-19。适用于本仓库的课程规划、学生材料、教师材料与 `ml-check`。本规范指导新内容编写；没有完成配套验收的目标，不能覆盖正在使用的课堂要求。

## 1. 课程目标与版本边界

学生应掌握借助 AI 提出问题、构建方案、获得证据和持续改进所需要的知识。AI 可以参与问题分析、方案设计、编程、计算、评价与写作；学生需要理解关键关系，作出有实质影响的选择，并用实验解释结果。不能只把原作业交给 AI，也不能把学生的角色缩小为检查 AI 的答案。

[课程说明](machine-learning-course/README.md)与[32 课目标地图](machine-learning-course/COURSE_MAP.md)定义新目标；本文件规定怎样编写和验收。[教师设计](course-instructor/COURSE_DESIGN.md)将目标落实到课堂，[教师索引](course-instructor/LESSON_INDEX.md)区分现行课包和重组任务，[发布说明](course-instructor/RELEASE.md)规定切换条件。这些文件出现分歧时，应在同一变更中消除分歧，不能任选一个执行。

**当前仍是过渡期。**尚未切换的课堂和 S01–S30 逐课材料对应[教学基线 84aa171](https://github.com/h-lu/machine-learning-course/blob/84aa1710803f006cf781590126cafcd494bf5515/machine-learning-course/COURSE_MAP.md)；新目标地图对应规划合并提交 `699abe0`。C01–C02 的成套候选课包及状态见 [入门课发布记录](course-instructor/releases/intro-2026-09-19.md)，合并候选材料不自动切换生产场次。新旧编号相同不代表内容相同。没有明确发布的新课包继续按原材料授课，不将旧题库完成情况当作新目标达成。

课程保持 16 周、32 次课、每次 90 分钟。两次入门课后为五个六课模块：问题与评价，学习、表示与泛化，预训练模型复用，模型、工具与人的协作，反馈、使用与迁移。每课完成一个小实验；五个正式模块各至少有一次同一作品的原方案与修订候选比较，允许依据新证据保留原方案。

## 2. 知识选择与掌握程度

选入必修的依据不是 AI 能否替人操作，而是缺少该知识会不会妨碍理解问题、构建方案、判断证据或迁移。每课围绕一个主要关系，明确学生要解释、计算、预测或诊断什么。

| 知识层次 | 必须达到的程度 | 不应作为默认要求 |
|---|---|---|
| 数据、概率、损失、评价、泛化、优化及相关与因果的区别 | 用小例子解释关系，核对关键计算，预计条件变化的影响 | 按算法名单逐一复写完整实现 |
| 模型能力来源、预训练模型复用、生成评价、AI 数据生产、工具协作与反馈 | 构建并比较合理替代方案，解释改变了什么及证据能支持什么 | 背提示模板、熟悉单个平台或只指出 AI 出错 |
| 数学与代码的实现 | 追踪一次关键输入、输出或参数更新，定位影响结论的环节 | 记忆接口参数、重复代码和超出核心关系的长篇推导 |

经典模型和微型网络承载必要机制，不再自动各占一课。深入架构、更多算法和完整推导可以拓展；不能把必学关系藏到选做，也不能把压缩课时理解为更快讲完旧清单。

## 3. 学生文案与固定结构

**全仓通用标准：面向初学者，使用机器学习社区通用术语，文字通俗。**这项要求适用于所有后续课程，不限于 C01–C02；覆盖学生 README、LEARN、数据说明、示例代码注释与报错、报告提示、教师对学生的讲解，以及概念题的题干、选项、解释和学习说明。

使用公认中文名称，首次出现时紧跟一句通俗解释；需要查阅资料时附常用英文或缩写。例如“特征（feature）是预测时使用的输入”“标签／目标值（label / target）是希望预测的真实结果”“平均绝对误差（MAE）是逐条绝对差的平均”。保留“训练集、验证集、测试集、基线模型、拟合、回归、分类、数据泄漏”等标准术语，不用自造名称或口号替代。可以说“最后才使用的数据”来解释测试集，但不能始终避开“测试集”这个名称。

先给一个学生能理解的情境，再解释术语，再用一行数据或小计算核对。一个段落集中说明一个关系；明确对象、时间、单位与分母，不用省略指代让读者猜。区分标签与预测值、模型训练与 AI 编程、MAE 与准确率、精确率与召回率。术语不能因追求口语而失真，也不能因追求专业而堆砌缩写。

必要前置知识在课内提供；新课不假定学生已会算法推导、JSON 或开发工具。命令说明在哪个目录执行、读取什么、生成什么、怎样核对和失败时怎么办。不要为了保持每课五个知识点引入尚未解释的概念。\n\n**冷读验收也是通用标准。**编写完成后，要按“第一次接触机器学习、只拥有仓库和本课页面”的学生视角从头阅读并实际照做。首个任务前只引入完成它所必需的概念；内部字段名、文件格式和工具术语在第一次操作时就地解释，不要求学生先读完整术语表或开发文档。步骤必须有清楚的先后顺序，并说明成功后先看哪个产物。报告模板用具体问题提示作答，不用抽象栏目让初学者猜教师想要什么。冷读中发现需要依赖教师口头补充才能继续的地方，应先修改材料，再把口头说明作为辅助。

术语表见 [学生术语表](course-student-template/docs/TERMINOLOGY.md)；中文解释与社区定义有疑问时，核对官方文档并记录来源。人工审阅应让读者能举例、计算或判断；术语出现次数、可读性分数和关键词检查不能证明文案易懂。

首屏回答：要做什么、为什么做、提交什么、失败怎么办。保留现有七个二级标题，顺序不变：

1. 本课要解决什么问题：说明使用者、输入、用途与限制。
2. 本课要学会什么：用 3–5 个相关概念解释本课的主要关系，并写出掌握表现。
3. 90 分钟安排：讲解、方案构建、比较检查、概念练习与提交。
4. 完成步骤：给出可运行起点，让学生选择影响结果的方法和检查。
5. 必须提交什么：程序、实际结果、主要比较、自设计检查与有依据的建议。
6. 不同起点怎么做：入门支持保留相同关键关系，提高任务增加深度。
7. 运行限制：数据、依赖、资源、时间及是否需要网络。

`LEARN.md` 必须有通俗解释、带数值或可追踪过程的最小例子、常见误区、何时该用或不该用，以及换情境判断题。不能以 API 列表、抽象术语或重复模板替代知识讲解。

继续遵守现有检查器的学生用语规则：写“任务说明”而不是“问题契约”，写“概念检查”而不是“知识自查”，写“适用场景与限制”而不是“适用范围”。把“样本单位”解释成“一行数据代表什么”，把“预测时点”写成“做预测时已经知道哪些信息”，把“错误代价”写成“误报或漏报会带来什么后果”。避免“门禁、签收、锁定、消费测试集、红组、蓝组、委托”等脱离上下文的说法。完整旧术语集合以 `ml-check/ml_check/checker.py` 的 `OLD_TERMS` 为准，不为放行文案随意放宽检查。

分层标题用中文在前：必做任务（Core）、入门支持（Support）、提高任务（Upgrade）、换数据重测（Transfer）、自选拓展（Open extension），并解释具体要做什么。文件名和既有 JSON 字段保留，但不能因此恢复旧说法。

## 4. 必做任务：构建与证据同时出现

每课应有用途与评价、实际运行、一个实质比较、自设计失败案例或条件变化，以及据此保留或修改的建议。C01 是入门例外：先完成最小流程与一条输入的核对，C02 再为同一作品加入替代方案比较；两课仍均不计平时分。

学生能改变问题、数据、表示、方法、评价或结果用途。至少一个选择改变实际实验或结论，不要求选教师的模型。原方案可以胜出，停止使用也可以有依据；不能强迫学生编造成功或为了“改进”无意义地增加复杂度。

先写预计结果，再借助 AI 提出候选方案，选择课堂内能完成的一项比较，运行并核对，再用新证据提出下一步。AI 也可以帮助提出检查方法；不要求设计“AI 猜不中”的陷阱，不以 AI 出错次数为难度。不收集完整 AI 对话，不以代码量、模型数或界面外观判分。

作品与理解分别检查。生成报告或重跑成功不能单独证明掌握；使用短计算、条件变化前的预计、解释一个样本或换情境诊断，检查学生能否迁移关系。

## 5. 教师参考与 A/B 概念题

每课 `RUNBOOK.md` 写明主要关系、前置知识、最小资源、90 分钟组织、观察问题和失败支持。`REFERENCE.md` 至少说明两条合理路线及各自条件，列出独立核算或反例，不把默认程序输出当唯一答案。

现行题库接口每课需要五个具体知识点，每个知识点一对 A/B 题。五点应围绕同一主要关系拆成可判断的问题，不能填入无关术语凑数。A、学习说明、B 使用同一概念对应；B 换情境而不是只换数字。AI 学习页只展示知识点与学习说明，不显示或复述 A/B 题。完成参与和答题正确率用途不同，具体计分仍按 `GRADING.md`。

课次重组必须同时调整知识点、题干、选项、解释、学习说明及服务题库版本。旧题不能只换课号就宣布适配。完整教师参考与答案按现有公开发布政策保留，不嵌入学生任务目录，也不宣称公开题库是保密考试。

## 6. 实验、资源与结论的限度

必做实验使用课前准备的普通 CPU、约 8 GB 可用内存与小型离线数据；单次运行目标不超过 3 分钟，从头重跑目标不超过 5 分钟。不要求学生购买服务、使用显卡或课堂下载大模型。实验离线与 AI 辅助、Gitea 概念检查的联网安排分开说明，教师提供接入支持或补做办法。

每课明确验证的是机制、已有响应还是新输出。小模型可用于追踪参数更新、注意力与遮罩，但不能证明真实用户或所有大模型中的效果。人工固定表示不能说成真实预训练表示，只更新输出头不能说成完整预训练。

新提示或改变后的资料必须有匹配的新输出。缓存只能比较其明确覆盖的候选；未覆盖的输入报告无结果，不能静默套用旧响应。改变多步流程后，生成步骤须接收实际改变后的输入。规则模拟动态选择仅说明模拟流程，不能冒充模型自主选择。

最后测试留到方案与评价规则确定之后。修改提示、模型、流程或评价器后，看过的测试结果只能作为开发证据，除非另有新测试。重复生成用于估计波动，不增加独立测试样本数；记录重复运行条件，区分推理耗时、缓存重放、实测与估算。AI 标注或评分须注明来源并抽查，不能由同一生成与评价过程自证有效。

随机输出先保存来源、输入、配置与原始输出，再确定性重算指标。现有 `scripts/course.py` 的 CI 逐文件比较产物哈希；本次不改变该行为。固定记录的重算能验证分析，不能代替新生成有效性的验证。缺少验证主要关系所需资源时暂缓发布，不能把必学内容改成只有付费学生才能完成的拓展。

## 7. 目录职责与兼容

`machine-learning-course/` 保存公开目标地图；`course-student-template/` 保存学生任务、数据与工具；`course-instructor/` 保存设计、参考、题库源文件与发布记录；`ml-check/` 保存材料检查器和在线概念服务。

本地 `ml_check` 做结构、路径与语法检查，不执行学生提交命令，也不替人评价结论。学生的 `scripts/course.py run/ci` 会执行代码；教师参考验证也会运行受信任程序。不能将这些工具一概描述为“不执行代码”，也不能在有生产凭据的环境执行未知学生程序。

保留 C01、C02、S01–S30 和 `lesson-01` 至 `lesson-32`，以及 `contract.json`、`submission.json`、原标签格式和现有成绩权重。此处的兼容仅指接口，不代表新旧内容同义。接口变更应单独审查，不能夹在教学文案中实施。

## 8. 按完整课包迁移和验收

新内容以目标地图为准编写，但正式切换必须成套处理：学生 README/LEARN、数据/代码、教师 RUNBOOK/REFERENCE、概念题、索引和发布记录一起对齐。优先完成 C01–C02 入门小批次，再推进五个正式模块；不得只改一份学习卡便算迁移完成。

每次发布记录适用班级、材料提交、题库版本、新旧内容对应、前置支持、已验收与待补部分。服务当前按课号加载单份题库，不会因写了版本备注自动隔离新旧场次；切换条件见 `course-instructor/RELEASE.md`。不覆盖旧标签、旧题目解释或成绩依据，不追溯增加已完成作业要求。

验证分层报告：文档检查只证明结构和链接；数学与软件测试证明对应计算；真实试教才支持 90 分钟可完成性的判断。完整课包发布前运行 `python3 tools/validate_course.py`、`PYTHONPATH=ml-check pytest -q ml-check/tests` 及新增目标的专项实验，并人工阅读自主选择、证据和理解检查。验证报告记录受检提交、命令、环境、结果和未运行项，不用旧报告证明新提交通过。

纯规范与索引变更可以先合并，但必须标明未切换课包。环境不足时如实报告实际检查范围，不用空文件、关键词命中或模拟成功替代未做的测试。课程文案与独立实验验收缺一不可。
