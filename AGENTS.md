# 机器学习课程内容编写规范

生效日期：2026-09-06。适用范围：本目录下的课程规划、学生仓库、教师仓库和 `ml-check`。本文件是重写课程内容时的唯一现行写作标准；旧版蓝图只用于追溯，不作为措辞或结构模板。

## 1. 课程目标

这是 16 周、32 次课的现代机器学习入门课。内容包括经典监督/无监督学习、深度学习、Transformer、LLM 基础与应用、强化学习，以及上线前测试和监控。学生可以全程使用 AI，但必须能说明：任务是什么、数据怎样得到、结果是否可信、什么时候不该使用模型。

每课都要产出一个可以运行、检查和解释的小项目。项目不能退化为“调用一个库、抄出一个分数”或“让 AI 写完代码”。AI 可以完成代码和计算；学生必须选择方案、设计检查、解释失败并决定是否继续使用。

## 2. 学生文案写作规则

学生 README、学习卡和练习题先用日常中文说明，再在第一次出现时给出社区英文。例如：

- “任务说明（task specification）”，不要写“问题契约”；
- “概念检查”，不要写“知识自查”；
- “采用决策”，不要写“使用决定”；
- “适用场景与限制”，不要写“适用范围”；
- “失败案例或边界情况测试”，不要写“容易失败的输入/极端条件”作为默认说法；
- “输入接口约定（input schema）”，不要写“输入契约”；
- “指标和行动标准”，不要写“指标契约”；
- “MDP 定义”，不要写“MDP 契约”；
- “条件变化”或“新数据更新”，不要单独使用不解释的 `Event`。

避免把 Core、Support、Upgrade、Transfer、Open extension 当作学生必须先理解的黑话。页面上的主标题统一写成：必做任务（Core）、入门支持（Support）、提高任务（Upgrade）、换数据重测（Transfer）、自选拓展（Open extension）。每次出现都要立即解释它要做什么。

避免“结果记录、锁定、门禁、签收、绿色、红组/蓝组、负责决定、委托、消费测试集”等没有上下文的口号式表达。统一改成具体动作：记录数据、指标和结论；先定好评测方法，最后测试数据只用一次；运行检查；比较指标；写出上线、继续试用或停止使用的理由。句子尽量短；一个段落只讲一个动作或一个判断。

常见说法也要直接换成学生听得懂的句子：“运行前预测”写成“先写下预计会看到的结果”；“可复现运行”写成“别人按 README 命令能重新得到主要结果”；“失败案例/边界情况测试”写成“设计一条容易让模型出错的输入”；“适用边界”写成“什么时候可以用、什么时候不能用”；“样本单位”写成“一行数据代表什么”；“预测时点”写成“做预测时已经知道哪些信息”；“行动规则”写成“结果出来后怎么做”；“错误代价”写成“误报或漏报会带来什么后果”；“换数据重测”写成“换一份数据或场景再跑一次”；“回退/撤回”写成“恢复旧版本/停止使用”。

学生页面的首屏必须能回答四句话：你要做什么？为什么做？请提交什么？遇到失败怎么办？不要在首屏连续堆放英文缩写；英文只在中文解释后用括号补充。教师内部文件可以保留 API、字段名和英文术语，但也要给出白话解释。

## 3. 每课固定结构

每个学生课次 README 必须按以下顺序写：

1. **本课要解决什么问题**：使用者、数据、行动和限制；
2. **本课要学会什么**：3–5 个概念，每个概念说明用途；
3. **90 分钟安排**：知识讲解、AI 实践、结果检查和提交时间；
4. **完成步骤**：学生按顺序执行的命令和编辑动作；
5. **必须提交什么**：程序、结果、失败案例、修改后的决定；
6. **不同起点怎么做**：Support / Core / Upgrade / Transfer 用完整中文解释；
7. **运行限制**：数据大小、运行时间、是否需要 GPU 或网络。

每课 `LEARN.md` 必须包含：通俗解释、一个最小例子、一个常见误区、何时该用/不该用、一个让学生自己判断的问题。不要只罗列 API 名称或英文术语。

## 4. 每课最低要求与分层

Core 是每位学生都要完成的实质任务，至少包括：

- 明确任务和评价指标；
- 运行一个可复现的基线或最小模型；
- 设计一个失败案例、边界情况测试或条件变化；
- 根据结果修改原来的判断，并说明理由；
- 在干净环境重新运行并保存结果。

Upgrade、Transfer、Open extension 只能增加比较、迁移或探索，不能把 Core 的关键概念和判断移到“选做”。每课 README 必须写清 Core 的具体完成标准，不能只写“完成四类证据”。

## 5. AI 使用方式

每课都安排“先写预计结果—AI 协助—运行—核对—修改”循环：

1. 学生先写下预计会看到什么结果；
2. 用 AI 解释概念、生成或修改代码；
3. 学生运行程序并核对数据、分母、指标和图表；
4. 设计一个 AI 不应直接猜中的失败案例；
5. 根据结果保留或修改决定。

不要求提交完整 AI 对话，也不以“AI 出错”作为难度来源。难度来自真实的模型选择、评价设计、资源限制和失败后的取舍。

## 6. 计算资源

Core 默认只用普通 CPU、约 8 GB 可用内存和离线数据包。单次运行目标不超过 3 分钟，从头重新运行不超过 5 分钟；不依赖付费 API、云 GPU 或课堂临时下载大模型。深度学习、LLM 和强化学习课使用小数据、固定权重、预计算 embedding、缓存 logits 或表格环境。更大模型和联网服务只能放在 Upgrade/Open extension。

## 7. 三个仓库的边界

- `machine-learning-course/`：公开课程规划，只写课程目标、32 课安排、学生需要知道的规则；
- `course-student-template/`：学生可读的任务、学习材料、数据和运行工具，不放教师答案、私有测试和后台说明；
- `course-instructor/`：教师运行单、参考实现、私有检查和发布说明；
- `ml-check/`：独立的格式、可复现性和内容底线检查，不替学生选择模型或代写分析。

文件名和 JSON 字段为已有接口时可以保留，例如 `contract.json`、`event.json`；首次出现必须用中文解释其用途，不得因此恢复“问题契约”等旧说法。

## 8. 重写和验收

重写时以 `machine-learning-course/COURSE_MAP.md` 的 32 行内容为准，同时更新学生 README/LEARN、教师 RUNBOOK/REFERENCE、题库和检查器提示。每次修改后必须：

1. 扫描学生可见文本中的禁用旧术语；
2. 验证 32 个课次编号和目录完整；
3. 运行学生测试、教师参考覆盖测试和 `ml-check` 测试；
4. 用严格模式检查学生、教师和课程规划仓库；
5. 在提交说明中记录改写范围和未改变的接口。

验收优先看学生能否读懂并完成任务，不以文件数量或术语数量作为质量指标。
